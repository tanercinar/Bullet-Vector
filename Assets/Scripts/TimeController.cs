using UnityEngine;

public class TimeController : MonoBehaviour
{
    void Update()
    {
        // Bu kod tamamen eğitim sürecini hızlandırmak için kullanılmıştır oynanışı etkilememesi için deaktif edilmiştir.
        /*
        // 1 tuşuna basınca normal hız (1x)
        if (Input.GetKeyDown(KeyCode.Alpha1))
        {
            Time.timeScale = 1f;
            Debug.Log("TimeScale: 1x (Normal)");
        }

        // 2 tuşuna basınca 5 kat hız (5x)
        if (Input.GetKeyDown(KeyCode.Alpha2))
        {
            Time.timeScale = 5f;
            Debug.Log("TimeScale: 5x");
        }

        // 3 tuşuna basınca 10 kat hız (10x)
        if (Input.GetKeyDown(KeyCode.Alpha3))
        {
            Time.timeScale = 10f;
            Debug.Log("TimeScale: 10x (Training Mode)");
        }

        // 0 tuşuna basınca durdur (Pause)
        if (Input.GetKeyDown(KeyCode.Alpha0))
        {
            Time.timeScale = 0f;
            Debug.Log("TimeScale: PAUSED");
        }
        */
    }
}
